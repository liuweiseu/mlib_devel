// xaui: Xcos block definition, PyQt6-mask pattern.
// Generated by casper_xps/scripts/gen_sci_block.py -- review the port
// topology and TODOs below before relying on this in a real design.
function [x, y, typ] = xaui(job, arg1, arg2)
    x=[];y=[];typ=[];
    select job
    case 'set' then
        x = arg1;
        export_exprs_to_tmpdir(x);
        bconfig = run_mask(x);
        x = update_exprs_from_tmpdir(x);
        x = xaui_update_ports(x, bconfig);
    case 'define' then
        tag = 'xaui';
        model = scicos_model();
        model.sim = list(tag, 4);
        model.blocktype = 'c';
        model.rpar = [];
        exprs = [];
        gr_i = [];
        model.in = [1, 2, 3];
        model.in2 = [8, 1, 1];
        model.out = [1, 2, 3];
        model.out2 = [8, 1, 1];
        // model.label doubles as the on-diagram display text (Scicos
        // aliases it with graphics.id); category is derived separately by
        // get_block_type.sci via file-probe, so this is free to be the name.
        model.label = tag;
        x=standard_define([13 15],model,exprs,gr_i)
        x.graphics.in_label = ['tx_data', 'tx_valid', 'tx_eof'];
        x.graphics.out_label = ['rx_data', 'rx_valid', 'rx_eof'];
        x.graphics.style = 'shape=rectangle;fillColor=yellow';
        /* init exprs */
        x = init_exprs(x);
        debug_info('xaui block loaded...')
    end
endfunction

function [x] = xaui_update_ports(obj, bconfigfn)
    // fixed port count: only widths/labels change, no set_io() needed
    // (same approach as swreg_update_ports)
    x = obj;
    bconfig = fromJSON(bconfigfn, 'file');
    p = bconfig('parameters');
    graphics = x.graphics;
    model = x.model;
    model.in = [1, 2, 3];
    model.in2 = [strtod(p('data_width')), 1, 1];
    model.out = [1, 2, 3];
    model.out2 = [strtod(p('data_width')), 1, 1];
    graphics.in_label = ['tx_data', 'tx_valid', 'tx_eof'];
    graphics.out_label = ['rx_data', 'rx_valid', 'rx_eof'];
    graphics.style = 'shape=rectangle;fillColor=yellow';
    x.graphics = graphics;
    x.model = model;
endfunction
