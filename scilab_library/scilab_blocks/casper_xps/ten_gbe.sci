// ten_gbe: Xcos block definition, PyQt6-mask pattern.
// Generated by casper_xps/scripts/gen_sci_block.py -- review the port
// topology and TODOs below before relying on this in a real design.
function [x, y, typ] = ten_gbe(job, arg1, arg2)
    x=[];y=[];typ=[];
    select job
    case 'set' then
        x = arg1;
        export_exprs_to_tmpdir(x);
        bconfig = run_mask(x);
        x = update_exprs_from_tmpdir(x);
        x = ten_gbe_update_ports(x, bconfig);
    case 'define' then
        tag = 'ten_gbe';
        model = scicos_model();
        model.sim = list(tag, 4);
        model.blocktype = 'c';
        model.rpar = [];
        exprs = [];
        gr_i = [];
        model.in = [1, 2, 3, 4];
        model.in2 = [64, 1, 1, 1];
        model.out = [1, 2, 3, 4];
        model.out2 = [64, 1, 1, 1];
        // model.label doubles as the on-diagram display text (Scicos
        // aliases it with graphics.id); category is derived separately by
        // get_block_type.sci via file-probe, so this is free to be the name.
        model.label = tag;
        x=standard_define([13 27],model,exprs,gr_i)
        x.graphics.in_label = ['tx_data', 'tx_valid', 'tx_eof', 'debug_rst'];
        x.graphics.out_label = ['rx_data', 'rx_valid', 'rx_eof', 'rx_bad_frame'];
        x.graphics.style = 'shape=rectangle;fillColor=yellow';
        /* init exprs */
        x = init_exprs(x);
        debug_info('ten_gbe block loaded...')
    end
endfunction

function [x] = ten_gbe_update_ports(obj, bconfigfn)
    // fixed port count: only widths/labels change, no set_io() needed
    // (same approach as swreg_update_ports)
    x = obj;
    bconfig = fromJSON(bconfigfn, 'file');
    p = bconfig('parameters');
    graphics = x.graphics;
    model = x.model;
    model.in = [1, 2, 3, 4];
    model.in2 = [64, 1, 1, 1];
    model.out = [1, 2, 3, 4];
    model.out2 = [64, 1, 1, 1];
    graphics.in_label = ['tx_data', 'tx_valid', 'tx_eof', 'debug_rst'];
    graphics.out_label = ['rx_data', 'rx_valid', 'rx_eof', 'rx_bad_frame'];
    graphics.style = 'shape=rectangle;fillColor=yellow';
    x.graphics = graphics;
    x.model = model;
endfunction
