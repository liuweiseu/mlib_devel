// dsp48e_bram_vacc: Xcos block definition, PyQt6-mask pattern.
// Generated by casper_dsp/scripts/gen_sci_block.py -- review the port
// topology and TODOs below before relying on this in a real design.
function [x, y, typ] = dsp48e_bram_vacc(job, arg1, arg2)
    x=[];y=[];typ=[];
    select job
    case 'set' then
        x = arg1;
        export_exprs_to_tmpdir(x);
        bconfig = run_mask(x);
        x = update_exprs_from_tmpdir(x);
        x = dsp48e_bram_vacc_update_ports(x, bconfig);
    case 'define' then
        tag = 'dsp48e_bram_vacc';
        model = scicos_model();
        model.sim = list(tag, 4);
        model.blocktype = 'c';
        model.rpar = [];
        exprs = [];
        gr_i = [];
        model.in = [1, 2];
        model.in2 = [1, 32];
        model.out = [1, 2];
        model.out2 = [1, 32];
        // model.label doubles as the on-diagram display text (Scicos
        // aliases it with graphics.id); category is derived separately by
        // get_block_type.sci via file-probe, so this is free to be the name.
        model.label = tag;
        x=standard_define([9 4],model,exprs,gr_i)
        x.graphics.in_label = ['new_acc', 'din'];
        x.graphics.out_label = ['valid', 'dout'];
        x.graphics.style = 'shape=rectangle;fillColor=#90EE90';
        /* init exprs */
        x = init_exprs(x);
        debug_info('dsp48e_bram_vacc block loaded...')
    end
endfunction

function [x] = dsp48e_bram_vacc_update_ports(obj, bconfigfn)
    // fixed port count: only widths/labels change, no set_io() needed
    // (same approach as swreg_update_ports)
    x = obj;
    bconfig = fromJSON(bconfigfn, 'file');
    p = bconfig('parameters');
    graphics = x.graphics;
    model = x.model;
    model.in = [1, 2];
    model.in2 = [1, strtod(p('n_bits_in'))];
    model.out = [1, 2];
    model.out2 = [1, strtod(p('n_bits_out'))];
    graphics.in_label = ['new_acc', 'din'];
    graphics.out_label = ['valid', 'dout'];
    graphics.style = 'shape=rectangle;fillColor=#90EE90';
    x.graphics = graphics;
    x.model = model;
endfunction
